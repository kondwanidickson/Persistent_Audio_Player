export default function Users(props) {
  return (
    <>
      <button onClick={props.PlayAudio}>Play Audio on USer page</button>
      <button onClick={props.pauseAudio}>Pause Audio on User page</button>
    </>
  );
}
